--path:"src"
--path:"../isonim/src"

# NH-M1: `isonim_freya/reactive_root` reaches `isonim/rxcore`, whose
# `isonim/core/platform` imports `nim_everywhere/platform`. Without this path
# the reactive core does not compile and the failure surfaces far away, as a
# cascade of `invalid type: 'ContextTable' in this context` errors out of
# `isonim/core/graph.nim`. isonim-gpui's `nim.cfg` already carries the same
# entry for the same reason (see the PLAT-19 note there).
--path:"../nim-everywhere/src"
